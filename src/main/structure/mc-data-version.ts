/** The Minecraft 1.21.1 NBT DataVersion stamped into every structure Blockwright writes
 *  (`.nbt`/`.schem`/`.litematic`) and reported in the AI schema. One source of truth, so a
 *  target-version bump is a single edit instead of ~10 scattered literals. */
export const DEFAULT_DATA_VERSION = 3955;
